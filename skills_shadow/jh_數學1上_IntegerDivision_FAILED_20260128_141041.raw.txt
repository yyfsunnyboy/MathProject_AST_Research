```python
import random

def fmt_num(x):
    return f"({x})" if x < 0 else str(x)

op_latex = {'+': '+', '-': '-', '*': '\\times', '/': '\\div'}

def clean_latex_output(q_str):
    # This is a placeholder function. In practice, you would implement
    # the actual cleaning logic here.
    return q_str

def generate(level=1, **kwargs):
    while True:
        abs_divisor_val = random.randint(2, 12)
        abs_quotient_val = random.randint(2, 10)
        
        dividend_sign = random.choice([-1, 1])
        divisor_sign = random.choice([-1, 1])
        
        if dividend_sign == 1 and divisor_sign == 1:
            continue
        
        abs_dividend_val = abs_divisor_val * abs_quotient_val
        dividend = dividend_sign * abs_dividend_val
        divisor = divisor_sign * abs_divisor_val
        
        result = dividend // divisor
        
        if (divisor != 0 and 
            dividend % divisor == 0 and 
            isinstance(result, int) and 
            1 <= abs(result) <= 10 and 
            (dividend < 0 or divisor < 0)):
            
            question_math = f"{fmt_num(dividend)} {op_latex['/']} {fmt_num(divisor)}"
            question_text = f"計算下列各式的值。{clean_latex_output(question_math)}"
            answer = str(result)
            return {'question_text': question_text, 'answer': answer, 'mode': 1}
```